# risc_v_single_cycle
This repository contains a custom-built, single-cycle RISC-V (RV32I) processor core implemented in *SystemVerilog* from scratch.  The project focuses on a strict modular RTL design, explicitly separating the Datapath and Control Unit without relying on pre-made templates.
